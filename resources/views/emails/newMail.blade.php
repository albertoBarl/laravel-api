<h2>NEW EMAIL</h2>
<div>
    Titolo:
    <p class="mb-2">{{ $lead->title }}</p>
    Slug:
    <p class="mb-2">{{ $lead->slug }}</p>
</div>

<div>
    Contenuto:
    <p>{{ $lead->content }}</p>
</div>
